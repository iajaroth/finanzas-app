# Credenciales del único usuario de la app
AUTH_EMAIL=jbadillasanchez@outlook.com
AUTH_PASSWORD=cambia-esto

# Secretos generados (openssl rand -hex 32)
JWT_SECRET=
APP_SECRET=

# URL pública de la app (para el callback OAuth de Outlook)
APP_URL=https://financiera.jbsautomation.online

# App registrada en Azure (portal.azure.com) para leer el correo
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT=common

DATA_DIR=./data
PORT=3000
